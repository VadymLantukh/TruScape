export interface CompanyRowProps { 
    id: number,
    catefory: string,
    compamy: string,
    status: 
}

const CompanyRow = ({
  id,
  category,
  company,
  status,
  promotion,
  country,
  joinedDate,
}) => {};

export default CompanyRow;
