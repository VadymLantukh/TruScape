export interface CompanyRowProps { 
    id: number,
    catefory: string,
    
}

const CompanyRow = ({
  id,
  category,
  company,
  status,
  promotion,
  country,
  joinedDate,
}) => {};

export default CompanyRow;
