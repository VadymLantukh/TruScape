export interface CompanyRowProps { 
    id: number,
    catefory: 
}

const CompanyRow = ({
  id,
  category,
  company,
  status,
  promotion,
  country,
  joinedDate,
}) => {};

export default CompanyRow;
